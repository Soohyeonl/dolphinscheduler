type AccessToken {
    id: ID!
    userId: Int!
    token: String
    expireTime: String
    createTime: String
    updateTime: String
}

type AccessTokenPageInfo {
    lists: [AccessToken]
    totalCount: Int
    totalPage: Int
    currentPage: Int
}

type QueryAccessTokenListResultType {
    code: Int
    msg: String
    data: AccessTokenPageInfo
}
